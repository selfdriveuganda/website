import { Spinner } from "../ui/spinner";

const LoadingComponent = () => (
	<div className="typography flex min-h-screen items-center justify-center pt-20">
		<Spinner />
	</div>
);

export default LoadingComponent;
